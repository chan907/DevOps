services:
  server:
    build: ./server
    ports:
      - "5001:8000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/Ecommerce

  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"